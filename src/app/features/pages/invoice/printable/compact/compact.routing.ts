import { Route } from '@angular/router';
import { CompactComponent } from 'app/features/admin/pages/invoice/printable/compact/compact.component';

export const compactRoutes: Route[] = [
    {
        path     : '',
        component: CompactComponent
    }
];
